# FT-005 — Anonymous + Device Tracking

## Thông tin

| Trường | Giá trị |
|--------|---------|
| ID | FT-005 |
| Tên | Anonymous + Device Tracking |
| Trạng thái | pending |
| Priority | medium |
| Ngày tiếp nhận | 2026-03-15 |

---

## Mô tả tính năng

Hỗ trợ user không đăng nhập vẫn có thể chat, đồng thời lưu device info để theo dõi online status.

**Requirements:**
- User anonymous có thể chat mà không cần đăng nhập
- Device info được lưu vào DB (browser, OS, timezone, etc.)
- User có thể login sau và conversations vẫn được giữ lại
- Track online status của thiết bị

---

## Database Schema

```prisma
model user {
  id            String   @id @default(uuid())
  email         String?  @unique
  password      String?
  firebase_uid  String?  @unique
  display_name  String?  @map("display_name")
  photo_url     String?  @map("photo_url")
  provider      String?  // "email" | "firebase" | null (anonymous)
  
  created_at    DateTime @default(now()) @map("created_at")
  updated_at    DateTime @updatedAt @map("updated_at")
  
  devices       device[]
  conversations conversation[]
  
  @@map("user")
}

model device {
  id                String   @id @default(uuid())
  user_id           String?  @map("user_id")
  user              user?    @relation(fields: [user_id], references: [id])
  
  device_id         String   @unique  // từ localStorage
  browser           String?
  os                String?
  language          String?
  timezone          String?
  screen_resolution String?
  ip_address        String?
  last_seen         DateTime @default(now()) @map("last_seen")
  is_online         Boolean  @default(false) @map("is_online")
  
  created_at        DateTime @default(now()) @map("created_at")
  updated_at        DateTime @updatedAt @map("updated_at")
  
  conversations     conversation[]
  
  @@map("device")
}

model conversation {
  id          String   @id @default(uuid())
  name        String
  user_id     String?  @map("user_id")
  user        user?    @relation(fields: [user_id], references: [id])
  device_id   String?  @map("device_id")
  device      device?  @relation(fields: [device_id], references: [id])
  
  created_at  DateTime @default(now()) @map("created_at")
  updated_at  DateTime @updatedAt @map("updated_at")
  
  message     message[]
  
  @@map("conversation")
}

model message {
  id              String       @id @default(uuid())
  conversation_id String       @map("conversation_id")
  conversation    conversation @relation(fields: [conversation_id], references: [id], onDelete: Cascade)
  role            String
  content         String
  created_at      DateTime     @default(now()) @map("created_at")
  
  @@index([conversation_id])
  @@map("message")
}
```

---

## Flow

### 1. Anonymous User

```
Frontend (localStorage)
┌─────────────────────────────────────┐
│ device_id = "device_xxx"            │
│ browser = "Chrome 122"               │
│ os = "MacIntel"                      │
│ timezone = "Asia/Ho_Chi_Minh"        │
└─────────────────────────────────────┘
        │
        ▼ (Header: X-Device-Info)
Backend
        │
        ▼ findOrCreateDevice()
Database: device table
```

### 2. Logged In User

```
Frontend
┌─────────────────────────────────────┐
│ Authorization: Bearer token         │
│ device_id = "device_xxx"            │
└─────────────────────────────────────┘
        │
        ▼
Backend: Link device → user
Database: device.user_id = user.id
```

---

## API Changes

### Update: POST /conversation/messages

**Headers:**
```
Authorization: Bearer <token>      // nếu login
X-Device-Info: <json>              // luôn luôn gửi
```

**Request Body:**
```json
{
  "message": "Hello",
  "conversation_id": "optional"
}
```

### New: GET /devices

Lấy danh sách thiết bị của user (login required)

### New: PATCH /devices/:id

Cập nhật device (rename, etc.)

---

## Dependencies

- FT-001: Chat Gemini Streaming (đang dùng)
- FT-002: Auth Firebase (đang dùng)

---

## Notes

- Device ID tạo từ localStorage + random
- Khi anonymous login → link device.user_id = user.id
- Online status update qua heartbeat hoặc khi có request mới